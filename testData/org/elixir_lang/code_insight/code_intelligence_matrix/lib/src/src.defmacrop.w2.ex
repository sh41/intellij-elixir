# Generated by generate.exs; do not edit. Regenerate with `mise exec -- elixir generate.exs` in testData/org/elixir_lang/code_insight/code_intelligence_matrix.
defmodule Src.Defmacrop.W2 do
  defmacrop src_defmacrop_w2_snoc(q, x) when is_list(q), do: quote(do: {unquote(q), unquote(x)})
  defmacrop src_defmacrop_w2_snoc(queue, item), do: quote(do: {unquote(queue), unquote(item)})
  defmacrop src_defmacrop_w2_snoc(q), do: quote(do: {unquote(q)})
  defmacrop src_defmacrop_w2_snoc(q, x, y), do: quote(do: {unquote(q), unquote(x), unquote(y)})

  def local_site(a, b), do: src_defmacrop_w2_snoc(a, b) # @local

  def uses(a, b) do
    {
      src_defmacrop_w2_snoc(a), # @uses_0
      src_defmacrop_w2_snoc(a, b, a), # @uses_1
      a,
      b
    }
  end
end
