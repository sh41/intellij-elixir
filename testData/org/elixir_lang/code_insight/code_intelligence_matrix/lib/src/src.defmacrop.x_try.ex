# Generated by generate.exs; do not edit. Regenerate with `mise exec -- elixir generate.exs` in testData/org/elixir_lang/code_insight/code_intelligence_matrix.
defmodule Src.Defmacrop.XTry do
  try do
    defmacrop src_defmacrop_x_try_snoc(q, x), do: quote(do: {unquote(q), unquote(x)})
  rescue
    _ -> nil
  end

  def local_site(a, b), do: src_defmacrop_x_try_snoc(a, b) # @local

  def uses(a, b) do
    {
      a,
      b
    }
  end
end
