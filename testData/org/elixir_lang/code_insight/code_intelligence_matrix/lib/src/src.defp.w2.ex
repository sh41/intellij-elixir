# Generated by generate.exs; do not edit. Regenerate with `mise exec -- elixir generate.exs` in testData/org/elixir_lang/code_insight/code_intelligence_matrix.
defmodule Src.Defp.W2 do
  defp src_defp_w2_snoc(q, x) when is_list(q), do: {q, x}
  defp src_defp_w2_snoc(queue, item), do: {queue, item}
  defp src_defp_w2_snoc(q), do: {q}
  defp src_defp_w2_snoc(q, x, y), do: {q, x, y}

  def local_site(a, b), do: src_defp_w2_snoc(a, b) # @local

  def uses(a, b), do: {src_defp_w2_snoc(a), src_defp_w2_snoc(a, b, a), a, b}
end
