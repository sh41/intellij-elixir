# Generated by generate.exs; do not edit. Regenerate with `mise exec -- elixir generate.exs` in testData/org/elixir_lang/code_insight/code_intelligence_matrix.
defmodule ExDbgi.Defp.W2 do
  defp ex_dbgi_defp_w2_snoc(q, x) when is_list(q), do: {q, x}
  defp ex_dbgi_defp_w2_snoc(queue, item), do: {queue, item}
  defp ex_dbgi_defp_w2_snoc(q), do: {q}
  defp ex_dbgi_defp_w2_snoc(q, x, y), do: {q, x, y}

  def local_site(a, b), do: ex_dbgi_defp_w2_snoc(a, b) # @local

  def uses(a, b) do
    {
      ex_dbgi_defp_w2_snoc(a), # @uses_0
      ex_dbgi_defp_w2_snoc(a, b, a), # @uses_1
      a,
      b
    }
  end
end
